# Protect all files in gallery/ by default
<FilesMatch "\.(enc|json)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Allow access to files.json
<Files "files.json">
    Order Allow,Deny
    Allow from all
</Files>
